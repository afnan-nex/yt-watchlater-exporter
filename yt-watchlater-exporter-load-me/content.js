function collectWatchLaterVideos() {  const videos = [];  document.querySelectorAll('ytd-playlist-video-renderer').forEach(video => {    const title = video.querySelector('#video-title')?.textContent?.trim();    const url = 'https://www.youtube.com' + video.querySelector('a#thumbnail')?.getAttribute('href');    if (title && url) {      videos.push({ title, url });    }  });  return videos;}function downloadJSON(data, filename) {  const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });  const url = URL.createObjectURL(blob);  const a = document.createElement('a');  a.href = url;  a.download = filename;  a.click();  URL.revokeObjectURL(url);}window.addEventListener('load', () => {  setTimeout(() => {    const list = collectWatchLaterVideos();    if (list.length > 0) {      downloadJSON(list, 'watch_later_videos.json');    } else {      alert('No videos found or content not fully loaded.');    }  }, 5000); // wait to ensure full load});